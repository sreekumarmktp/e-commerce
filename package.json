{
  "name": "e-commerce-app",
  "version": "1.0.0",
  "description": "E-commerce web application with React frontend and Node.js backend",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:ui\"",
    "dev:api": "cd API && npm run dev",
    "dev:ui": "cd UI && npm start",
    "build": "cd UI && npm run build",
    "install:all": "npm install && cd API && npm install && cd ../UI && npm install",
    "test": "cd API && npm test && cd ../UI && npm test",
    "prepare": "husky install"
  },
  "keywords": ["e-commerce", "react", "nodejs", "typescript"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.2.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.8.1"
  }
}
